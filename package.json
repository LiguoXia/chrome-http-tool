{
  "name": "http-tool",
  "version": "1.0.0",
  "private": true,
  "description": "HTTP interface invocation & trigger tool with scene management (data persisted in ./data)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "jsdom": "^30.1.0"
  }
}
